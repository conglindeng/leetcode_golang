package difficult

func Calculate(s string) int {
	// char: + - ( )
	// num
	// state machine

	for i := 0; i < len(s); i++ {
		c := s[i]
		if c == '+' {

		} else if c == '-' {

		} else if c == '(' {

		} else if c == ')' {

		} else {

		}
	}
//todo:dcl
	return 0
}
